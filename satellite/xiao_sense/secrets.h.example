// Copy to secrets.h (gitignored) and fill in. The satellite joins the same
// LAN as the adapter and streams to the adapter host's UDP port.
#pragma once
#define WIFI_SSID      "your-ssid"
#define WIFI_PASSWORD  "your-password"
#define ADAPTER_HOST   "192.168.0.165"   // PC running the dog adapter (docker publishes 5005/udp)
#define ADAPTER_PORT   5005
