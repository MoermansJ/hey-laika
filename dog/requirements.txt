# Pinned to the versions validated on 2026-09-02 (pip freeze of the dev
# venv) so the Docker build is reproducible. Bump deliberately.
Flask==3.1.3
Flask-CORS==6.0.5
gunicorn==23.0.0 ; sys_platform != "win32"
python-dotenv==1.2.3
requests==2.34.2
anthropic==1.1.0
pyserial==3.5
websocket-client==1.9.1
SQLAlchemy==2.0.52
# Ears: faster-whisper (CTranslate2, CPU int8) for the voice relay.
faster-whisper==1.2.1
# Eyes: JPEG decode + letterbox for the YOLO ONNX detector (onnxruntime and
# numpy already arrive with faster-whisper).
Pillow==12.3.0
